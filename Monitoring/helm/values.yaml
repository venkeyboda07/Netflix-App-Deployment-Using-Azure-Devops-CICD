kube-prometheus-stack:

  grafana:
    enabled: true
    adminUser: admin
    adminPassword: Admin@123

    service:
      type: LoadBalancer

  prometheus:
    service:
      type: LoadBalancer

    prometheusSpec:
      serviceMonitorSelectorNilUsesHelmValues: false
